package strategy

// Conversion first string is Microservice IP + Port, second string is rabbitmq exchange name join with route key,
// third string is the number of rabbitmq
//func Conversion(originGenerate []string) map[string]map[string]string {
//	rs := make(map[string]map[string]string)
//	head := originGenerate[0:8]
//
//	for i := 0; i < len(originGenerate); i++ {
//
//	}
//}
//
//func headConversion(head []string) []string {
//	typeOfChain := head[0:4]
//	theNumberOfChain := head[4:8]
//
//}
